name: Run tests
on:
  push:
    branches: [master]

# depends-on:
jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - run: go test -race -v -cover ./...
